# {{ ansible_managed }}

/var/log/monit.log {
    missingok
    notifempty
    monthly
    minsize 1M
    create 0644 root root
    postrotate
        /bin/kill -HUP `cat /var/run/monit.pid 2>/dev/null` 2> /dev/null || true
    endscript
}
